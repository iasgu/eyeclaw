# Skyvern depends on rich<14 while browser-use==0.12.7 requires rich==14.3.1.
# Install it in a separate virtual environment instead of the main EyeClaw .venv:
#   python -m venv D:\Codex\downloads\browser-automation-source\skyvern-venv
#   D:\Codex\downloads\browser-automation-source\skyvern-venv\Scripts\python.exe -m pip install -r requirements-skyvern.txt
skyvern>=1.0,<2
click>=8.4,<9
